class Solution {
    public boolean containsDuplicate(int[] arr) {
       Arrays.sort(arr);
            for(int i=1;i<arr.length;i++)
            {
                    if(arr[i]-arr[i-1]==0)
                    {
                            return true;
                    }
            }
            return false;
    }
}
